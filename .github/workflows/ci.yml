name: CI Pipeline

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install poetry
        # Ajout de --no-root pour ignorer l'installation du projet lui-même
        poetry install --no-root

    - name: Lint with flake8
      run: |
        # On utilise poetry run pour s'assurer que flake8 est celui du projet
        poetry run flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

    - name: Test with pytest
      env:
        # On définit une clé bidon juste pour que les tests passent
        FERNET_KEY: 'test-key-for-ci-environment'
      run: |
        # On utilise poetry run pour s'assurer que pytest utilise ton environnement virtuel
        poetry run pytest
